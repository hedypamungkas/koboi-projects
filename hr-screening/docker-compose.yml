services:
  koboi:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports: ["8002:8000"]
    volumes: ["koboi-data:/data"]
    env_file:
      - /Users/mekari/Documents/Research-POC/ai-agent-sample/koboi-agent/.env
  web:
    build: ./frontend
    ports: ["3002:80"]
    depends_on: [koboi]

volumes:
  koboi-data:
