# Copy to .env (or point docker-compose.yml's env_file at your own secrets file) and fill in
# real values. Never commit real credentials.
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=
