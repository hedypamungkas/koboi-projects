# Copy the real values from your own secrets store -- never commit a filled-in .env.
# docker-compose.yml in this project points at an absolute .env path outside this repo
# (see README.md); this file just documents which variables config/agent.yaml expects.

OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=
