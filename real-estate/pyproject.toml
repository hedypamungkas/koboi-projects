[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "realestate-ext"
version = "0.1.0"
description = "Harbor Realty Group -- custom koboi-agent tools for buyer chat + nightly listing automation."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
