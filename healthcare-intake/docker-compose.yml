services:
  koboi:
    build:
      context: ..
      dockerfile: healthcare-intake/backend/Dockerfile
    ports: ["8004:8000"]
    volumes: ["koboi-data:/data"]
    env_file:
      - /Users/mekari/Documents/Research-POC/ai-agent-sample/koboi-agent/.env
  web:
    build: ./frontend
    ports: ["3004:80"]
    depends_on: [koboi]

volumes:
  koboi-data:
