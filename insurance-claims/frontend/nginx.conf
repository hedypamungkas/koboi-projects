# no-cache so a web-container rebuild is always picked up on reload (304 revalidation still allowed).
server {
    listen 80;
    root /usr/share/nginx/html;
    add_header Cache-Control "no-cache";
    location / {
        index index.html;
    }
}
