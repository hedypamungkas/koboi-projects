# Copy this to a .env file and fill in real values, OR point
# docker-compose.yml's `env_file:` at an existing koboi-agent .env that
# already has these set (see README for how this repo's compose file does it).

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=
