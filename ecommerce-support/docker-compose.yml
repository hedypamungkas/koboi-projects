services:
  koboi:
    build:
      context: ..
      dockerfile: ecommerce-support/backend/Dockerfile
    ports: ["8001:8000"]
    volumes: ["koboi-data:/data"]
    env_file: ["/Users/mekari/Documents/Research-POC/ai-agent-sample/koboi-agent/.env"]
  web:
    build: ./frontend
    ports: ["3001:80"]
    depends_on: [koboi]
volumes:
  koboi-data:
